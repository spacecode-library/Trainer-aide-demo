// Solo practitioner session runner - reuses trainer session runner
// This allows solo practitioners to run sessions the same way trainers do
import TrainerSessionRunnerPage from '@/app/trainer/sessions/[id]/page';

export default TrainerSessionRunnerPage;
