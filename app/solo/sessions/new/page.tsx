// Solo practitioner new session page - reuses trainer new session page
// This allows solo practitioners to start sessions the same way trainers do
import TrainerNewSessionPage from '@/app/trainer/sessions/new/page';

export default TrainerNewSessionPage;
