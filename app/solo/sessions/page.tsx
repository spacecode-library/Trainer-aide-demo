// Solo practitioner sessions page - reuses trainer sessions page
// This allows solo practitioners to manage sessions the same way trainers do
import TrainerSessionsPage from '@/app/trainer/sessions/page';

export default TrainerSessionsPage;
