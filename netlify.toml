[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase timeout for AI generation (max is 60s on Business plan)
  timeout = 80
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"
