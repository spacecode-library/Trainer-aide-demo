[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Timeout controlled by Netlify plan tier (Business = 60s max, cannot be configured)
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-nextjs"
